import { Surface, Text } from "react-native-paper";
import globalStyles from "../globals/styles";

const Brands = () => {
    return (
        <Surface style={globalStyles.container}>
            <Text style={globalStyles.text}>Brands Screen</Text>
        </Surface>
    );
};

export default Brands;
