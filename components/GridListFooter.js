import { StyleSheet, View } from "react-native";

const GridListFooter = () => (
    <View style={gridListFooterStyles.container}></View>
);

const gridListFooterStyles = StyleSheet.create({
    container: {
        padding: 10,
    },
});

export default GridListFooter;
