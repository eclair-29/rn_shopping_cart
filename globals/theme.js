import { DefaultTheme } from "react-native-paper";

const theme = {
    ...DefaultTheme,
    roundness: 1,
    version: 3,
    colors: {
        ...DefaultTheme.colors,
        primary: "#006064",
        accent: "#006064",
    },
};

export default theme;
